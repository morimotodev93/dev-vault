# better-sqlite3 Troubleshooting

If `pnpm install` fails while building `better-sqlite3`,
the package may require a local native build environment.

On Windows, this can include:

- Python
- Microsoft C++ Build Tools
- Windows SDK

These requirements may vary depending on your Node.js version,
OS, architecture, and package version.
